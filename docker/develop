# This container serves the flask application in develop mode, which auto
# updates on changes for easy development.
# In this case I've opted to use the official NBCI Blast+ image, as it comes
# with BLAST as well as python3, and then just install the flask dependencies.

# https://hub.docker.com/r/ncbi/blast/tags
FROM ncbi/blast:2.10.1

# create dir
RUN mkdir /code
COPY /molmod/requirements.txt /code

WORKDIR /code

RUN pip3 install -r requirements.txt

ENV FLASK_ENV="develop"
CMD ["python3", "wsgi.py"]
